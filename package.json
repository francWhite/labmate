{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "scripts": {
    "run:ui": "lerna run --scope=labmate start",
    "run:api": "lerna run --scope=labmate-api start",
    "run:all": "lerna run --parallel start"
  }
}
